# Если MDEditorModelAdmin не найден, используйте следующий способ:
# 1. Импортируйте MDEditorWidget вручную.
# 2. Создайте кастомную форму для News с этим виджетом.
# 3. Подключите форму в NewsAdmin.

# Пример кода для admin.py:

from mdeditor.widgets import MDEditorWidget
from django import forms
from .models import News

class NewsAdminForm(forms.ModelForm):
    class Meta:
        model = News
        fields = '__all__'
        widgets = {
            'content': MDEditorWidget(),
        }

# Затем в NewsAdmin:
# class NewsAdmin(admin.ModelAdmin):
#     form = NewsAdminForm
#     ...

# Это гарантирует появление markdown-редактора в админке.
